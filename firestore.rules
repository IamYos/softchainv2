rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Block all client reads/writes. All access is via Admin SDK in API routes.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
